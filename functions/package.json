{
  "name": "flood-monitor-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "echo \"No lint configured\"",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions,database,firestore"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1",
    "twilio": "^5.5.3"
  }
}
